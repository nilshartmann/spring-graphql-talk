type Beer  {
    id: ID!
    name: String!
    """
    Price must not be higher than **ten euros**
    """
    price: String!
}

type Query {
    beer(beerId: ID): Beer
    beers: [Beer!]!
}

type Rating {
  id: ID!

  comment: String!
  stars: Int!
}

extend type Beer {
    ratings: [Rating!]!
}
