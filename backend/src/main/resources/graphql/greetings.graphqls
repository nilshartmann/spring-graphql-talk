type Greeting {
    # Der Name eines Greetings
    name: String!
    msg: String!

    greet: String!
}

extend type Query {
    # Liefert ein Greeting zurück
    hello(name: String!, msg: String!): Greeting!
}